name: Fase 66S · Jet Stream 200 hPa horizonte completo premium

on:
  workflow_dispatch:
  push:
    branches: [phase66s-jet200-full-horizon]
    paths:
      - "scripts/jet_200_full_phase66s.py"
      - "scripts/jet_250_full_phase66r.py"
      - "scripts/build_phase66s_maplibre_viewer.py"
      - "scripts/smoke_phase66s_maplibre.py"
      - "scripts/synoptic_250_full_phase66o.py"
      - "scripts/ecmwf_pressure_levels_phase4.py"
      - ".github/workflows/phase66s-jet200-full-horizon.yml"
      - "requirements.txt"

permissions:
  contents: read
  actions: read

jobs:
  generate:
    name: ${{ matrix.label }}
    runs-on: ubuntu-latest
    timeout-minutes: 300
    strategy:
      fail-fast: false
      matrix:
        include:
          - model: ecmwf
            label: ECMWF · Jet 200 hPa · 85 mapas · +360 h
            expected: 85
            horizon: 360
            artifact: fase66s-ecmwf-jet200-full
          - model: gfs
            label: GFS · Jet 200 hPa · 129 mapas · +384 h
            expected: 129
            horizon: 384
            artifact: fase66s-gfs-jet200-full
          - model: icon
            label: ICON-EU · Jet 200 hPa · 93 mapas · +120 h
            expected: 93
            horizon: 120
            artifact: fase66s-icon-eu-jet200-full
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"
      - name: Instalar dependencias
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Validar sintaxis y conversiones de altura
        run: |
          python -m py_compile scripts/jet_200_full_phase66s.py scripts/jet_250_full_phase66r.py scripts/synoptic_250_full_phase66o.py scripts/ecmwf_pressure_levels_phase4.py
          python - <<'PY'
          import sys
          import numpy as np
          sys.path.insert(0, 'scripts')
          import gfs_pressure_phase24 as g
          import ecmwf_pressure_levels_phase4 as e
          assert float(g.to_height_m(np.array([[11800.]],dtype='float32'),'m')[0,0]) == 11800.0
          z=e.geopotential_height(np.array([[115718.47]],dtype='float32'),'m**2 s**-2')
          assert abs(float(z[0,0])-11800.0) < 2.0
          print('Conversiones de altura GFS/ECMWF OK')
          PY
      - name: Generar horizonte completo Jet 200 hPa
        run: python scripts/jet_200_full_phase66s.py "${{ matrix.model }}"
      - name: Validar datos, cadencia y mapas Jet 200 hPa
        env:
          MODEL: ${{ matrix.model }}
          EXPECTED: ${{ matrix.expected }}
          HORIZON: ${{ matrix.horizon }}
        run: |
          python - <<'PY'
          import json, os
          from pathlib import Path
          m=os.environ['MODEL']; expected=int(os.environ['EXPECTED']); horizon=int(os.environ['HORIZON'])
          p=Path('experimental-phase66s')/m/'manifest-phase66s.json'
          d=json.loads(p.read_text(encoding='utf-8'))
          assert d['phase']=='66S' and d['status']=='ok' and d['level_hpa']==200, d.get('failure_notes')
          assert d['variable']=='jet_stream_wind_speed_geopotential'
          assert d['summary']=={'successes':expected,'failures':0,'expected':expected}, d['summary']
          assert d['horizon_hours']==horizon and max(d['generated_steps'])==horizon
          assert d['height_validation']=={'source':'Fase 66P 200 hPa validada','mean_min_m':9000,'mean_max_m':16500}
          if m=='ecmwf': wanted=list(range(0,145,3))+list(range(150,361,6))
          elif m=='gfs': wanted=list(range(0,385,3))
          else: wanted=list(range(0,79))+list(range(81,121,3))
          assert d['generated_steps']==wanted
          assert len(list(p.parent.glob('*.webp')))==expected
          for sk,r in d['maps'].items():
              assert r['status']=='ok', (sk,r)
              s=r['size']; assert s['width']>s['height'] and s['width']>=1000, (sk,s)
              assert 9000 <= r['geopotential_height_mean_m'] <= 16500, (sk,r['geopotential_height_mean_m'])
              wr=r['wind_speed_range_kmh']
              assert 0 <= wr['min'] <= wr['max'] <= 650, (sk,wr)
              assert wr['max'] >= 60, (sk,wr)
              assert len(r['source_requests']) >= 3
              assert (p.parent/r['image']).is_file(), r['image']
          print(f"{m} 66S validado {expected}/{expected} · Jet 200 hPa · horizonte +{horizon} h")
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: experimental-phase66s/${{ matrix.model }}/
          retention-days: 7
          if-no-files-found: error

  viewer:
    name: MapLibre · Jet 200 hPa · 307 mapas · navegador real
    needs: generate
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: actions/download-artifact@v4
        with:
          name: fase66s-ecmwf-jet200-full
          path: phase66s-input/ecmwf
      - uses: actions/download-artifact@v4
        with:
          name: fase66s-gfs-jet200-full
          path: phase66s-input/gfs
      - uses: actions/download-artifact@v4
        with:
          name: fase66s-icon-eu-jet200-full
          path: phase66s-input/icon
      - name: Construir visor MapLibre 66S
        run: |
          python -m py_compile scripts/build_phase66s_maplibre_viewer.py
          python scripts/build_phase66s_maplibre_viewer.py
      - name: Validar paquete Jet 200 hPa 307/307
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          out=Path('experimental-phase66s')
          d=json.loads((out/'report-phase66s.json').read_text(encoding='utf-8'))
          assert d['phase']=='66S' and d['status']=='ok' and d['level_hpa']==200
          assert d['variable']=='jet_stream_wind_speed_geopotential'
          assert d['production_changed'] is False
          assert d['total_maps']==307
          assert {k:v['maps'] for k,v in d['models'].items()}=={'ecmwf':85,'gfs':129,'icon':93}
          assert {k:v['horizon'] for k,v in d['models'].items()}=={'ecmwf':360,'gfs':384,'icon':120}
          assert len(list(out.glob('*/*.webp')))==307
          html=(out/'index.html').read_text(encoding='utf-8')
          for token in ('maplibregl.Map','OpenFreeMap','aspect-ratio:16/9','window.__phase66sState','Jet Stream 200 hPa','Dominio','km/h'):
              assert token in html, token
          assert 'leaflet' not in html.lower()
          print('66S paquete estático OK · Jet 200 hPa · 307/307 · MapLibre')
          PY
      - name: Instalar Chromium de prueba
        run: |
          python -m pip install --upgrade pip
          pip install playwright==1.55.0
          python -m playwright install --with-deps chromium
      - name: Probar horizontes finales e intervalos Jet 200 hPa
        run: |
          python -m py_compile scripts/smoke_phase66s_maplibre.py
          python -m http.server 8776 --directory experimental-phase66s >/tmp/phase66s-http.log 2>&1 &
          SERVER_PID=$!
          trap 'kill "$SERVER_PID" || true' EXIT
          sleep 2
          python scripts/smoke_phase66s_maplibre.py
      - uses: actions/upload-artifact@v4
        with:
          name: fase66s-maplibre-jet200-full-horizon
          path: experimental-phase66s/
          retention-days: 7
          if-no-files-found: error
