name: Generar ECMWF 700 hPa

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "scripts/ecmwf_pressure_levels_phase4.py"
      - "requirements.txt"
      - ".github/workflows/generate-700hpa.yml"

permissions:
  contents: read

jobs:
  ecmwf-700hpa-fase5:
    runs-on: ubuntu-latest
    timeout-minutes: 35

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Instalar dependencias
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Generar ECMWF 700 hPa
        env:
          PRESSURE_LEVELS: "700"
          FORECAST_STEPS: "0,12,24"
        run: python scripts/ecmwf_pressure_levels_phase4.py

      - name: Guardar mapas 700 hPa
        uses: actions/upload-artifact@v4
        with:
          name: meteorologia-mapas-ecmwf-700hpa
          path: public-pressure/
          if-no-files-found: error
          retention-days: 7
